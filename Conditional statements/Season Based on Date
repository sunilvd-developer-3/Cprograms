#include <stdio.h>

int main(){
    int month,day;
    scanf("%d%d",&month,&day);

    if((month==3 && day>=1)||(month>3 && month<6)||(month==5 && day<=31))
        printf("Spring");
    else
        printf("Other Season");
}
