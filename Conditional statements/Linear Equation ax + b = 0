#include <stdio.h>

int main(){
    int a,b;
    scanf("%d%d",&a,&b);

    if(a==0 && b==0)
        printf("Infinite solutions");
    else if(a==0)
        printf("No solution");
    else
        printf("x = %.2f",(float)-b/a);
}
